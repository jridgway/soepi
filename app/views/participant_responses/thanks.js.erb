window.onbeforeunload = null;
$('body').append("<%= escape_javascript(render(:template => '/participant_responses/_thanks.html.haml')) %>");
$('#thanks').dialog({modal:true, height:225, width:450});
if($('#question-slider').length > 0) {
  $('#question-slider, #tester').remove();
  $('#header, #breadcrumbs, #content-above-top, #content-top, #body_content, #body_content_left, #body_content_right, ' +
  '#footer, #uservoice-feedback').show('blind');
}