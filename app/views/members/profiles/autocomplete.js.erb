<%= @members.collect do |member|
  {
    :label => member.nickname,
    :logo => avatar(member, 40, false),
    :description => member.tags.join(', '),
    :value => member.nickname
  }
end.to_json.html_safe %>